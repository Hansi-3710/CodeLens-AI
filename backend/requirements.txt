# Pinned to versions verified working in this build (every provider class
# was actually constructed and exercised against these exact versions —
# see the LLM provider construction check run during the final hardening
# pass). Floor pins with a same-major-version ceiling: allows pip to pull
# patch/minor bug fixes without silently jumping to an untested future
# major version that could break an API this code depends on.
#
# Why this matters here specifically: the original pins in this file were
# written from training-data assumptions about "current" versions and were
# already stale by the time this was tested for real — openai had moved
# 1.x->2.x, huggingface_hub 0.x->1.x, transformers 4.x->5.x. All three were
# re-checked against these actual installed versions and the exact method
# signatures this code calls (chat.completions.create, text_generation,
# pipeline) are confirmed present and unchanged.

# Web framework
fastapi>=0.139.0,<1.0.0
uvicorn[standard]>=0.51.0,<1.0.0
pydantic>=2.13.0,<3.0.0
pydantic-settings>=2.14.0,<3.0.0

# Database
sqlalchemy>=2.0.51,<3.0.0
psycopg[binary]>=3.3.0,<4.0.0
alembic>=1.18.0,<2.0.0

# Auth
passlib==1.7.*
bcrypt==4.0.1  # pinned exact: passlib 1.7.4 is incompatible with bcrypt>=4.1's version probing
python-jose[cryptography]>=3.5.0,<4.0.0
python-multipart>=0.0.20,<1.0.0  # required by FastAPI's OAuth2PasswordRequestForm (login)
email-validator>=2.3.0,<3.0.0    # required for pydantic EmailStr
slowapi>=0.1.10,<1.0.0           # rate limiting

# LLM clients — the highest-scrutiny pins in this file. Each provider
# class (app/llm/providers/*.py) was constructed and its exact method
# calls checked against these installed versions' real signatures.
openai>=2.45.0,<3.0.0
huggingface-hub>=1.23.0,<2.0.0
transformers>=5.13.0,<6.0.0
torch>=2.13.0,<3.0.0

# Analysis
radon>=6.0.0,<7.0.0
flake8>=7.3.0,<8.0.0

# ML
sentence-transformers>=5.6.0,<6.0.0
scikit-learn>=1.8.0,<2.0.0
umap-learn>=0.5.12,<1.0.0
numpy>=2.4.0,<3.0.0
pandas>=3.0.0,<4.0.0
scipy>=1.17.0,<2.0.0

# Sandbox
docker>=7.2.0,<8.0.0

# Testing
pytest>=9.1.0,<10.0.0
pytest-asyncio>=1.4.0,<2.0.0
pytest-json-report>=1.5.0,<2.0.0
pytest-cov>=7.1.0,<8.0.0
httpx>=0.28.0,<1.0.0
